# Local dev config for Insight chat
# Server-side URL that the Next.js API route will call
INSIGHT_AGENT_URL=https://insight-agent-full.fly.dev/ask
# Optional: if your endpoint requires a bearer token, uncomment and set it
# INSIGHT_AGENT_KEY=your_secret_token_here

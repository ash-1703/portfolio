// Querying with "sanityFetch" will keep content automatically updated
// Before using it, import and render "<SanityLive />" in your layout, see
// https://github.com/sanity-io/next-sanity#live-content-api for more information.
// NOTE:
// The Live Content API is optional. In this project we don't currently use
// `sanityFetch` or `<SanityLive />` anywhere, and recent dependency changes can
// cause SanityClient type identity conflicts during `next build`.
// Keeping this module as a no-op avoids breaking production builds.

export {};
